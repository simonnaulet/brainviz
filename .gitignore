# Python-generated files
__pycache__/
.ipynb_checkpoints/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Données extraites / archives
dataset/
archives/

# Artefacts nnU-Net (données converties, preprocessing, checkpoints)
nnunet/
.torchinductor/

# Prétraitements et runs Rep-SliceMix
artifacts/

# Checkpoints de modèles entraînés
*.pt
!checkpoints/
!checkpoints/*.pt

# Sorties brutes des runs d'entraînement (poids + logs) ; les résumés utiles
# sont recopiés à la main dans report/ (voir report/*_summary.json)
runs/

# Sorties régénérables du rapport historique
report/figures/
report/*.aux
report/*.log
report/*.out
report/*.pdf
